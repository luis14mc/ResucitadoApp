# ============================================================
# ResucitadoApp v2 — Backend Django
# Parroquia Cristo Resucitado · Tegucigalpa, Honduras
# Python 3.12+ recommended
# ============================================================

# Core
Django==5.0.6
djangorestframework==3.15.2

# DB / Cloud
psycopg2-binary==2.9.9
dj-database-url==2.2.0

# CORS (Flutter → Django)
django-cors-headers==4.4.0

# Envs / Settings
python-decouple==3.8

# Producción / Railway
gunicorn==22.0.0
whitenoise==6.7.0

# Proxy de lecturas litúrgicas (scraping fuentes públicas)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2

# Utilidades
Pillow==10.4.0          # ImageField (santos, eventos, banners)
python-dateutil==2.9.0  # parsing fechas litúrgicas

# Admin polish (opcional pero útil para la Pastoral de Medios)
django-admin-interface==0.28.8
